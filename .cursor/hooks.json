{
  "version": 1,
  "hooks": {
    "stop": [
      {
        "command": "npx ng lint flights && npx ng test flights --configuration ci && npx ng build flights",
        "timeout": 600,
        "loop_limit": 3
      }
    ]
  }
}
